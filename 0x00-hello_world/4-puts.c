#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
   char alx_str[] = "Programming is like building a multilingual puzzle";

   puts(alx_str);
   return (0);
}
