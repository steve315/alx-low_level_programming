#include "main.h"
#include <stdlib.h>
/**
 * _abs - computes the absolute value of an integer
 * @n: input parameter
 * Return: 0 on success
 */
int _abs(int n)
{
	int absolute_n;

	absolute_n = abs(n);
	return (absolute_n);
}
